# IndieTracks 爬虫开发计划书

> 基于 grill-with-docs 质询会话生成 | 2026-05-17
> 更新：2026-06-09 — 添加 Linux 部署脚本
> 目标站：dizzylab.net | 框架：Scrapy | 数据库：PostgreSQL 18 | 对象存储：MinIO

---

## 一、数据库最终状态（14 张表）

### 1.1 去重键

| 表 | 去重列 | 类型 | 说明 |
|:---|:---|:---|:---|
| `albums` | `dizzylab_id` | VARCHAR(50) UNIQUE | Dizzylab 专辑 slug，如 `"SW20"` |
| `circles` | `dizzylab_labelid` | INT UNIQUE | Dizzylab 社团内部 ID |
| `users` | `dizzylab_user_id` | INT UNIQUE | Dizzylab 用户数字 ID（从 `/u/{id}` 提取） |
| `tags` | `name` | VARCHAR(50) UNIQUE | 标签名（已去 `#`） |

### 1.2 完整建表 SQL

- **建表**：`database/create_database.sql`（14 张表 + 16 条索引，`IF NOT EXISTS`）
- **清空数据**：`database/init.sql`（TRUNCATE 所有表 + 重置序列）
- **Python 工具**：`crawler/setup_db.py`（自动连接 PostgreSQL 执行建表 SQL）

---

## 二、数据源与 XPath / API 映射

### 2.1 首页 API — 专辑列表和社团基础信息

```
GET https://www.dizzylab.net/apis/getdiscs/?l={start}&r={end}&sort=ad&type=album
```

| JSON 字段 | 数据库字段 | 说明 |
|:---|:---|:---|
| `id` | `albums.dizzylab_id` | 如 `"SW20"` |
| `title` | `albums.title` | `"冬日咏叙调"` |
| `price` | `albums.price` | `40.0` |
| `cover` | `albums.cover_url` | CDN 完整 URL |
| `label` | `circles.name` | `"Static World"` |
| `labelid` | `circles.dizzylab_labelid` | `30` |
| `labelcover` | `circles.logo_url` | |
| `tags[]` | `tags.name`（去除前导 `#`） | `["#纯音乐","Static World","#东方project"]` |

翻页：`l` 起始位置，`r` 结束位置。`l=0&r=24` → 第 1 批，`l=24&r=48` → 第 2 批。

### 2.2 专辑详情页 HTML — `/d/{slug}/`

| 数据库字段 | XPath / 取值规则 | 已验证样例 |
|:---|:---|:---|
| `albums.title` | `//title/text()` 去掉末尾 `" - dizzylab"` | `"冬日咏叙调"` |
| `albums.info_title` | 所有 `<p>` 段落文本，`\n` 拼接 | 专辑描述长文 |
| `albums.info_content` | 所有 `<h3 class='text-left'>` 文本，`\n` 拼接 | "Track List" 等区段标题 |
| `albums.publish_date` | 匹配文本 `"发布于(\d{4})年(\d{1,2})月(\d{1,2})日"` | `"2026-05-01"` |
| `albums.cover_url` | `//img[@id='imgsrc0']/@data-src`（懒加载） | CDN URL |

#### 2.2.1 曲目列表（播放控件）

**容器**：`//ul[contains(@class,'playlist--list')]/li`

每个 `<li>` 结构：
```html
<li data-id="0" data-audio="https://streaming.dizzylab.net/.../preview/1.mp3">
  <span class="t-title">1. 冬临之时 - Static World (02:48)</span>
</li>
```

| 数据库字段 | XPath | 解析规则 |
|:---|:---|:---|
| `sort_order` | `li/@data-id` | `data-id + 1`（0-based → 1-based） |
| `file_name` | `li//span[@class='t-title']/text()` | 去掉序号前缀，正则提取曲名部分 |
| `track_length` | 同上 | 正则提取 `(MM:SS)` |
| `object_key` | `li/@data-audio` → 下载 → 上传 MinIO | `audio/preview/{slug}/{sort_order:03d}.mp3` |

#### 2.2.2 标签

**XPath**：`//h4[@class='text-left']/a/text()`

原始文本：`"##纯音乐"` `"#Static World"` `"#治愈系"` `"##东方project"`
处理：`strip("#")` → `"纯音乐"` `"Static World"` `"治愈系"` `"东方project"`

#### 2.2.3 支持的人（已购买）— AJAX API

```
GET https://www.dizzylab.net/albums/getbuyers/?discid={slug}&l={start}&r={end}
```

| JSON 字段 | 数据库 | 说明 |
|:---|:---|:---|
| `names[]` | `users.username` | |
| `user_url[]` | `users.dizzylab_user_id` | 从 `/albums/u/90162` 提取数字 |
| `avatar_url[]` | `users.avatar_url` | 去外层引号 |

#### 2.2.4 评论 — AJAX API

```
GET https://www.dizzylab.net/albums/getdisccomment/?discid={slug}&l={start}&r={end}
```

| JSON 字段 | 数据库 | 说明 |
|:---|:---|:---|
| `disc_comment[]` | `comments.content` | |
| `user_names[]` | 关联 `users` | |
| `user_url[]` | `users.dizzylab_user_id` | |
| `avatar_url[]` | `users.avatar_url` | |

### 2.3 社团列表 — AJAX API

```
GET https://www.dizzylab.net/apis/getlabels/?l={start}&r={end}&sort=ad
```

### 2.4 社团详情页 HTML — `/l/{name}/`

成员解析：`//p[text()='成员']/following-sibling::div//a[contains(@href, '/u/')]`

### 2.5 Setup 页 — 用户角色

```
GET https://www.dizzylab.net/setup
```

STAFF/PRO 用户角色解析。

### 2.6 用户维页面（无需登录）

| 页面 | URL 格式 | 分页参数 | 对应表 |
|:---|:---|:---|:---|
| 已购专辑 | `/u/{id}/music/` | `?page=N` | `owned_albums` |
| 收藏 | `/u/{id}/likes/` | `?dp=N` | `favorites` |
| 关注社团 | `/u/{id}/following/` | `?dp=N` | `circle_follows` |

---

## 三、配置文件体系

### 3.1 延迟策略 `crawler/config/delay.json`

```json
{
  "active": "default",
  "strategies": {
    "default": {
      "download_delay": 3,
      "between_albums_min": 60,
      "between_albums_random": 60,
      "between_tracks_min": 2,
      "between_tracks_random_max": 5,
      "concurrent_requests_per_domain": 1
    }
  }
}
```

### 3.2 数据库配置 `crawler/config/database.json`

```json
{
  "host": "localhost",
  "port": 5432,
  "database": "indietracks",
  "user": "postgres",
  "password": "postgres"
}
```

### 3.3 爬虫行为控制 `crawler/config/spider.json`

```json
{
  "mode": "full",
  "max_albums": 50,
  "max_users": 1000
}
```

### 3.4 MinIO 连接配置 `crawler/config/minio.json`

```json
{
  "endpoint": "http://localhost:9000",
  "access_key": "minioadmin",
  "secret_key": "minioadmin",
  "bucket": "indietracks",
  "prefixes": {
    "audio_preview": "audio/preview/",
    "audio_full": "audio/full/",
    "covers": "covers/",
    "logos": "logos/",
    "avatars": "avatars/"
  }
}
```

---

## 四、Scrapy 项目文件结构

```
crawler/
├── config/                    # 配置文件
│   ├── delay.json             # 延迟策略
│   ├── database.json          # 数据库连接
│   ├── spider.json            # 爬虫行为控制
│   └── minio.json             # MinIO 连接
├── indietracks_spider/
│   ├── items.py               # 12 个 Scrapy Item
│   ├── pipelines.py           # PostgreSQL Pipeline（注册表调度）
│   ├── repository.py          # 统一持久化层（FK 缓存 + Upsert）
│   ├── item_registry.py       # Item 注册表
│   ├── db_mixin.py            # DbSpiderMixin（统一 DB 连接）
│   ├── settings.py            # 延迟加载 config JSON
│   └── utils/                 # 工具模块
│       ├── config_loader.py   # 统一配置读取
│       ├── constants.py       # 共享常量
│       ├── db.py              # DbConnection 上下文管理器
│       ├── delay.py           # 统一延迟控制器
│       ├── storage.py         # StorageBackend（MinioBackend/MemoryBackend）
│       ├── circle.py          # 社团详情解析
│       ├── minio.py           # 向后兼容代理
│       └── parsing.py         # 共享解析函数
├── spiders/                   # 爬虫（7 个）
│   ├── album_base.py          # 专辑爬虫基类
│   ├── album_test.py          # 测试爬虫（10 张）
│   ├── album_bulk.py          # 批量爬虫
│   ├── album_incremental.py   # 增量爬虫
│   ├── circle.py              # 社团爬虫
│   ├── user_roles.py          # 用户角色爬虫
│   └── user_pages.py          # 用户维页面爬虫
├── tests/                     # 单元测试（195 个用例）
├── requirements.txt           # scrapy + psycopg2-binary + minio
└── scrapy.cfg
```

---

## 五、爬虫设计文档

### 5.1 `album_test.py` — 测试爬虫

- 不受 `spider.json` 控制，固定爬前 10 张专辑
- 子请求链路：album detail → buyers → comments → circle_detail

### 5.2 `album_bulk.py` — 批量爬虫

- 首次数据铺底 + 后续全量刷新
- 遵守 `spider.json`（mode + max_albums）
- 子请求链路：专辑详情 → 曲目下载 → 标签 → 社团 → 购买者 → 评论
- 专辑间延迟：`reactor.callLater` 非阻塞

### 5.3 `album_incremental.py` — 增量爬虫

- 从 API 第一页开始，遇到已存在专辑立即停止
- 不受 `spider.json` 控制

### 5.4 `circle.py` — 社团爬虫

- 从 DB 读取社团列表，逐一爬详情页
- 补充描述、logo、成员 ID

### 5.5 `user_roles.py` — 用户角色爬虫

- 从 `/setup` 页获取 STAFF/PRO 用户列表
- 填充 `users.user_role`

### 5.6 `user_pages.py` — 用户维页面爬虫

- 爬取已购专辑、收藏、社团关注
- 支持指定用户 ID 或从 DB 读取

---

## 六、Pipeline 写入策略

Pipeline 使用 `item_registry` 注册表调度：

| 步骤 | 表 | 去重方式 |
|:---|:---|:---|
| 1 | `tags` | `ON CONFLICT (name) DO UPDATE` |
| 2 | `circles` | `ON CONFLICT (dizzylab_labelid) DO UPDATE` |
| 3 | `users` | `ON CONFLICT (dizzylab_user_id) DO UPDATE`（role 保护） |
| 4 | `albums` | `ON CONFLICT (dizzylab_id) DO UPDATE` |
| 5 | `album_tags` | `ON CONFLICT DO NOTHING` |
| 6 | `album_circles` | `ON CONFLICT DO NOTHING` |
| 7 | `work_files` | `DELETE + INSERT`（按 album_id 清旧数据） |
| 8 | `comments` | `ON CONFLICT DO NOTHING` |
| 9 | `owned_albums` | `ON CONFLICT DO NOTHING` |
| 10 | `user_circles` | `ON CONFLICT DO NOTHING` |
| 11 | `favorites` | `ON CONFLICT DO NOTHING` |
| 12 | `circle_follows` | `ON CONFLICT DO NOTHING` |

---

## 七、执行命令

### Linux

```bash
# 一键部署爬虫环境
bash scripts/linux/setup-crawler.sh

# 运行爬虫启动器
bash scripts/linux/run-crawlers.sh

# 手动运行
cd crawler
source env/bin/activate
scrapy crawl album_bulk
```

### Windows

```bash
cd crawler
env\Scripts\activate

# 测试爬虫
scrapy crawl album_test

# 批量爬虫
scrapy crawl album_bulk

# 增量爬虫
scrapy crawl album_incremental

# 社团爬虫
scrapy crawl circle

# 用户角色
scrapy crawl user_roles

# 用户维页面
scrapy crawl user_pages
```

---

## 八、爬虫执行优先级

| 顺序 | 爬虫 | 前置依赖 | 用途 |
|:---|:---|:---|:---|
| 1 | `album_bulk`（full, max=N） | MinIO 已启动 | 首批数据铺底 + 音频下载 |
| 2 | `album_incremental` | 首期数据已有 | 日常追新 |
| 3 | `circle` | circles 表有数据 | 社团描述+logo+成员 |
| 4 | `user_roles` | users 表有数据 | 角色标记补充 |
| 5 | `user_pages` | users/albums/circles 有数据 | 用户已购/收藏/关注 |
| 6 | `album_bulk`（full, max=0） | MinIO 已启动 | 最终全量刷新 |

---

## 九、合规声明

- 请求间隔 ≥ 60 秒（配置可调），单域名并发 = 1
- 仅采集公开可访问页面，无需登录
- 数据仅用于课程考核，不得商用
- 遵循 dizzylab robots.txt

---

文件位置：`database/create_database.sql`
